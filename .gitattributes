# Normalize all text files to LF in the repo and on checkout.
* text=auto eol=lf

# PowerShell is friendlier to Windows hosts with CRLF.
*.ps1 text eol=crlf

# Binary dataset blobs — never touch line endings.
*.n2k -text

# Firebase hosting cache files are machine-generated.
*.cache -text
